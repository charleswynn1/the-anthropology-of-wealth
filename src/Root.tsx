import { Composition } from "remotion";

// Register active project compositions here during development.
// This file is tracked as an empty shell — local composition entries
// are ignored by git (skip-worktree) and removed in W12 on completion.
export const RemotionRoot = () => {
  return <></>;
};
